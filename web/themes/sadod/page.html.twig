{#
/**
 * @file
 * Default theme implementation to display a single page
 *
 * doctype, html, head and body tags can be found in the html.html.twig
 *
 * General utility variables:
 * - base_path: base URL path of the Drupal install, usually "/"
 * - is_front: indicating if the current page is the front page.
 * - logged_in: indicating if the user is registered and signed in.
 * - is_admin: indicating if the user has admin permission
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - messages: Status and error messages. Should be displayed prominently.
 * - node: loaded node, if node associated with the page and node ID is second argument in path
 *   (node/12345/revisions, but not comment/reply/12345)
 *
 * @see template_preprocess_page()
 * @see html.html.twig
 *
 * @ingroup themeable
 */
#}
<div class="layout-container">

  <header>
    {{ page.header }}
  </header>

  {{ page.navbar }}

  <main class="layout-content">
    <a id="main-content" tabindex="-1"></a>
    {{ page.content }}
  </main>
  
  {% if page.content_secondary %}
    {{ page.content_secondary }}
  {% endif %}

  {% if page.content_tertiary %}
    {{ page.content_tertiary }}
  {% endif %}

  {% if page.sidebar %}
    <aside class="sidebar">
      {{ page.sidebar }}
    </aside>
  {% endif %}

  {% if page.footer %}
    <footer>
      {{ page.footer }}
    </footer>
  {% endif %}

</div>